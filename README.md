# dreamxxx24runpodsimple.json

Dockerized ComfyUI workflow: dreamxxx24runpodSimple.json

## Contents

- `Dockerfile` - Docker container configuration for running this ComfyUI workflow
- `example-request.json` - Example API request payload for testing

## Deployment to RunPod Serverless

This repository is configured for deployment on RunPod Serverless endpoints.

### Quick Deploy

1. In RunPod console, go to **Serverless** → **New Endpoint**
2. Select **Import Git Repository**
3. Choose `charliefairbairn/dreamxxx24runpodsimple.json`
4. Configure your GPU and settings
5. Deploy!

### API Request Format

The base image `runpod/worker-comfyui:5.5.0-base` includes the handler code. Send requests with your workflow:

```json
{
  "input": {
    "workflow": {
      // Your ComfyUI workflow nodes
    }
  }
}
```

See `example-request.json` for a complete example.

### Automatic Rebuilds

When you push changes to the `main` branch, RunPod automatically rebuilds and deploys the new version.

Generated by [ComfyUI to Docker Wizard](https://comfy.getrunpod.io)
